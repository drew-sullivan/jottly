// Production discovery is intentionally fail-closed. Add only reviewed authored package IDs and
// their exact canonical Solo definition digests in the same deployment as their featured package.
export const communityAllowlist = Object.freeze([]);
