// The scheduled sweep will preserve the last known good catalog until at least six reviewed,
// client-validated source packages are committed here. Never use synthetic games in production.
export const featuredCommunityPackages = Object.freeze([]);
